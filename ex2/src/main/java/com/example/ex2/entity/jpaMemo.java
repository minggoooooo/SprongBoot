package com.example.ex2.entity;

public class jpaMemo {
    /*
    *  CRUD 메서드
    * insert : save(엔티티 객체)
    * select : findById(키 타입), getOne(키타입)
    * update : save(엔티티 객체) -> 없다면 insert 있다면 수정을 해서 둘 다 save를 씀
    * delete : deleteById(키 타입), delete(엔티티 객체)
    * */
}
