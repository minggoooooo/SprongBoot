package com.example.ex2.entity;

public class propertiesMemo {
    // jpa 맨 위에거는 - 프로젝트 실행 시에 자동으로 DDL(create, alter, drop 등)을 생성할것인지를 결정하는 설정
    // 설정 값에는 create , update , create-drop, validate가 있음
    // 예를 들어 create의 경우 매번 테이블 생성을 새로 시도함
    // 중간거 - hibernate가 동작하면서 발생하는 sql을 포맷팅해서 출력함. sql의 가독성을 위한 것
    // 마지막거 - jpa 처리 시에 발생하는 sql을 보일것인지 설정
}
